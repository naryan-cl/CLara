import { ComingSoon } from "@/components/ComingSoon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin Queue"
      description="Review documents flagged needs_review, fix missing OKF metadata, and manage stream settings like the isolation toggle."
    />
  );
}
